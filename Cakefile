{ generate, serve } = require '../forbair'

task 'index.html', 'build the index page', generate
task 'style.css', 'build the stylesheet', generate
task 'app.js', 'build the application script', generate
task 'index.js', 'build the package index script', generate
task 'start', serve
